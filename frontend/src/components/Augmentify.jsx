import React from 'react';

const Augmentify = () => {
    return (
        <div>
            <iframe
                title="ArView"
                src="/ArView.html"
                style={{
                    border: 'none',
                    width: '100%',
                    height: '800px',
                    backgroundColor: '#dab5be', // Change this to set the iframe background color
                }}
            />
        </div>
    );
};

export default Augmentify;
